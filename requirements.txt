# Fijado a partir del log de build de Render del 1 sept 2026 (línea
# "Successfully installed ..."), no de memoria ni de versiones
# "recomendadas" — es exactamente lo que ya corre en producción.
# Ver CHANGELOG.md (auditoría, Etapa 9) para el porqué.
#
# Al actualizar una dependencia a propósito, vuelve a fijar TODO el
# archivo con el log de build del deploy siguiente (build_command en
# render.yaml no cambia; pip seguirá resolviendo transitivas nuevas
# si tocas alguna de las 9 de abajo).

# ── Dependencias directas (las que usa el código de la app) ──────────
flask==3.1.3
psycopg2-binary==2.9.12
openpyxl==3.1.5
gunicorn==26.2.0
apscheduler==3.11.3
pytz==2026.3.post1
Pillow==12.3.0
requests==2.34.2
pypdf==6.16.2
# (2026-09-04, v12.32.16) pdfplumber: para leer el Listado de Pedidos
# DETALLADO de SAP como tabla real (pypdf desordena las columnas en este
# formato — ver comentario junto a _extraer_listado_detallado_completo() en
# app.py). Versiones fijadas contra lo instalado en este entorno de
# desarrollo, NO contra un log de build de Render real — a diferencia del
# resto de este archivo, esto SÍ hay que volver a fijar con el log de
# build del primer deploy que incluya este cambio (ver nota de cabecera).
pdfplumber==0.11.9
pdfminer.six==20251230
pypdfium2==5.7.1
cryptography==46.0.7
# (2026-09-07, v12.32.42) pytesseract: lectura por OCR del PDF de pedido oficial
# cuando llega firmado/sellado — al escanear el papel firmado pierde toda su
# capa de texto (ver comentario junto a _ocr_texto_pdf_pedido_oficial() en
# app.py), así que se rasteriza cada página con pdfplumber/pypdfium2 (ya
# dependencias de arriba, sin coste nuevo de rasterizado) y se lee esa
# imagen con Tesseract vía este paquete. El binario de Tesseract en sí
# (`tesseract-ocr` + `tesseract-ocr-spa`) NO es un paquete de pip — se
# instala a nivel de sistema operativo en el `Dockerfile` (ver ese archivo y
# `render.yaml`, que pasa a runtime Docker en esta misma entrega porque el
# runtime nativo de Python de Render no permite instalar paquetes del
# sistema). Versión fijada contra lo instalado en este entorno de
# desarrollo, NO contra un log de build de Render real — igual que
# pdfplumber arriba, esto SÍ hay que volver a fijar con el log del primer
# deploy Docker que incluya este cambio (ver nota de cabecera del archivo).
pytesseract==0.3.13

# ── Transitivas (dependencias de las de arriba) — fijadas también   ──
# ── para que el deploy instale SIEMPRE lo mismo, no solo lo directo ──
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
et-xmlfile==2.0.0
idna==3.19
itsdangerous==2.2.0
jinja2==3.1.6
markupsafe==3.0.3
tzlocal==5.4.4
urllib3==2.7.0
werkzeug==3.1.8
